name: tech_state_management
description: 状态管理方案 — setState、InheritedWidget、ChangeNotifier、Bloc 对比
version: 0.0.1
publish_to: none

environment:
  sdk: ">=3.11.0 <4.0.0"

dependencies:
  flutter:
    sdk: flutter
  provider: ^6.1.2
  flutter_riverpod: ^2.6.1
  go_router: ^14.0.0
  interview_core:
    path: ../../common/core
  interview_widgets:
    path: ../../common/widgets
