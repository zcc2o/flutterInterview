enum InterviewRoutes {
  home('/'),
  timer('/timer'),
  router('/router'),
  state('/state'),
  di('/di'),
  http('/http'),
  painter('/painter'),
  animation('/animation'),
  storage('/storage');

  final String path;
  const InterviewRoutes(this.path);
}
